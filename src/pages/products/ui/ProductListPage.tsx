import { ProductListUi } from "@/src/entities/product";
import { FC } from "react";

export const ProductListPage:FC = () => {
    return (
        <div>
            <ProductListUi/>
        </div>
    )
}