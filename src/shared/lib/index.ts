/**
 * Use within a form on a button to ignore form action
 * 
 * @example
 * ```tsx
 * <form action={...}>
 *   <button 
 *     formAction={ignoreFormAction} 
 *     onClick={...}
 *   >
 *     ...
 *   </button>
 * </form>
 * ```
 */
export function ignoreFormAction() {}